version: "2"

run:
  timeout: 5m

linters:
  default: standard
  settings:
    staticcheck:
      checks:
        - "all"
        - "-QF*"
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
    paths:
      - testdata

formatters:
  enable:
    - gofmt
